pipeline {
    agent any
    stages {
        stage("Variables") {
            steps {
                echo "env:  ${env.getEnvironment()}"
            }
        }
        stage("Version Check") {
            steps {
                sh "node --version"
                sh "npm --version"
                sh "git --version"
            }
        }
    }
}